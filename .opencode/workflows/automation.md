# Automation Workflow

Test Designer → Automation Engineer → Execute → Failure Analyzer → Debugger if product/test issue → Retest → Report.

Reuse the existing framework unless a new framework is explicitly justified.
